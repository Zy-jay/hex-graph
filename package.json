{
  "name": "hexv2",
  "license": "MIT",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ hexican",
    "create-local": "graph create --node http://localhost:8020/ hexican",
    "remove-local": "graph remove --node http://localhost:8020/ hexican",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 hexican"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.37.6",
    "@graphprotocol/graph-ts": "^0.29.1"
  },
    "resolutions": {
    "assemblyscript": "git+https://github.com/AssemblyScript/assemblyscript.git#v0.6"
  }
}
